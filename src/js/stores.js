import { writable } from 'svelte/store';

//New audience
export const createdNewAudienceData   = writable([]);
export const createdNewAudiencePeople   = writable([]);

export const peopleAudienceData   = writable([]);
export const createdCommunityData = writable([]);
export const createdAudienceData  = writable([]);
export const createdDriversData   = writable([]);
export const exportFieldsData     = writable([]);
export const responseFieldsData   = writable([]);
export const previewTagsData      = writable([]);
export const createTagsData       = writable([]);
export const createPostData       = writable([]);


export const previewTagsResponseData = writable([]);